services:
  app:
    ports:
      - 3000:8080
    build:
      context: api

  e2e:
    build:
      context: e2e
      dockerfile: Dockerfile
    environment:
      # Overrides the default baked into e2e/Dockerfile.
      - URL=http://app:8080
    depends_on:
      - app
